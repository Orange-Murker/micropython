# Only use spaces to indent your .yml configuration.
# -----
# You can specify a custom docker image from Docker Hub as your build environment.
image: stronglytyped/arm-none-eabi-gcc

pipelines:
  default:
    - step:
        caches:
          - mpy-build
          - stm-build
        script:
          - apt update
          - apt install libffi-dev pkg-config python3 curl -y
          - cd mpy-cross
          - make
          - cd ../ports/stm32
          - make submodules
          - make BOARD=NUCLEO_H743ZI
          - pipe: atlassian/bitbucket-upload-file:0.1.8
            variables:
              BITBUCKET_USERNAME: $BITBUCKET_USERNAME
              BITBUCKET_APP_PASSWORD: $BITBUCKET_APP_PASSWORD
              FILENAME: "build-NUCLEO_H743ZI/firmware.hex"
              ACCOUNT: 'ctw-bw'
              REPOSITORY: 'micropython'

definitions:
  caches:
    mpy-build: mpy-cross\build
    stm-build: ports\stm32\build-NUCLEO_H743ZI